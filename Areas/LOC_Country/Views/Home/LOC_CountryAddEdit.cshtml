@{
    ViewData["Title"] = "Country Add/Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model AddressBook_Replica.Areas.LOC_Country.Models.LOC_CountryModel;

<div class="row">
    <div class="col-md-12">

        <h2>@TempData["LOC_Country_Insert_Msg"]</h2>
        <div class="container">
            <h2>Country</h2>
            <hr />
            <form method="post" asp-area="LOC_Country" asp-controller="LOC_Country">

                @Html.HiddenFor(x => x.CountryID)

                <div class="form-group row">
                    <label class="col-sm-2 col-form-label">Country Name</label>
                    <span asp-validation-for="CountryName" class="text-danger"></span>
                    <div class="col-sm-4">
                        <input type="text" class="form-control" placeholder="CountryName" asp-for="CountryName">
                    </div>
                </div>

                <div class="form-group row">
                    <label class="col-sm-2 col-form-label">Country Code</label>
                    <span asp-validation-for="CountryCode" class="text-danger"></span>
                    <div class="col-sm-4">
                        <input type="text" class="form-control" placeholder="CountryCode" asp-for="CountryCode">
                    </div>
                </div>

                @Html.HiddenFor(x => x.CreationDate)
                @Html.HiddenFor(x => x.ModificationDate)
                <input type="hidden" class="form-control" asp-for="UserID" value="@ViewBag.UserID" />

                <div class="row">
                    <div class="col-4">
                        <button type="submit" class="btn btn-success rounded-pill" asp-action="Save">Save</button>
                        &nbsp;
                        <a class="btn btn-danger rounded-pill" asp-action="Index">Cancel</a>
                    </div>
                </div>

            </form>
        </div>
    </div>
</div>

@section Scripts{
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}